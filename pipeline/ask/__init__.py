"""Retrieval-augmented /ask backend for SAICA-KG.

Exposes a small FastAPI service that the static site's /ask page POSTs to.
Kept out of the static build so the site still builds without the backend.
"""
